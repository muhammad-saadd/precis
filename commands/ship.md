---
description: "Atomic commit workflow. Stage specific files, write a why-focused commit message, bump version if VERSION exists, create PR on feature branches. Use --quick to skip version bump and PR."
---

Invoke the `precis:ship` skill. Pass any arguments directly: $ARGUMENTS
