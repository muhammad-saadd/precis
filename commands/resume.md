---
description: "Restore context from the last session using git history. One paragraph output. Use --quick for a one-sentence summary."
---

Invoke the `precis:resume` skill. Pass any arguments directly: $ARGUMENTS
